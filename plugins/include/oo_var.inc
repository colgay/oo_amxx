#if defined _oo_var_included
	#endinput
#endif

#define _oo_var_included

#include <oo>

native oo_var_array(const class[], const name[], size=1, arrsize=1);

native oo_var_trie(const class[], const name[], size=1);

native oo_var_object(const class[], const name[], size=1);